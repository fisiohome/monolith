# Example: Each entry references a location (from locations.yml) and a service (from services.yml).
# We mark them all "active: true," but you can customize as needed.

jakarta_selatan_fisiohome:
  location: jakarta_selatan # => references "jakarta_selatan" in locations.yml
  service: fisiohome # => references "fisiohome" in services.yml
  active: true
  created_at: <%= Time.zone.now %>
  updated_at: <%= Time.zone.now %>

jakarta_selatan_pusat_okupasi:
  location: jakarta_selatan # => references "jakarta_selatan" in locations.yml
  service: pusat_okupasi # => references "pusat_okupasi" in services.yml
  active: true
  created_at: <%= Time.zone.now %>
  updated_at: <%= Time.zone.now %>

bandung_fisiohome:
  location: bandung # => references "bandung" in locations.yml
  service: fisiohome # => references "fisiohome" in services.yml
  active: true
  created_at: <%= Time.zone.now %>
  updated_at: <%= Time.zone.now %>

bandung_pusat_okupasi:
  location: bandung # => references "bandung" in locations.yml
  service: pusat_okupasi # => references "pusat_okupasi" in services.yml
  active: true
  created_at: <%= Time.zone.now %>
  updated_at: <%= Time.zone.now %>
